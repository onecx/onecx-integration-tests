ifdef::env-site[]
include::partial$_attributes.adoc[]
endif::[]
:page-hide-prev:

= Integration Tests

== Overview

OneCX Integration Tests is a TypeScript-based runner that starts a OneCX platform stack with Testcontainers, validates container health, and optionally runs End-to-End (E2E) tests.

The module supports local and CI execution and produces reproducible run artifacts.

== Purpose

Use this module to:

* Start core, service, BFF, and UI containers in a predictable order
* Validate configuration before startup
* Run health checks and optional heartbeat monitoring
* Execute an E2E container as the final runtime step when configured
* Export runtime metadata and structured artifacts for debugging and reporting

The version semantics for docs and runtime contracts are strict:

* `latest` means the last released product version
* `main` means the latest build from the main branch
* Ambiguous or missing tag resolution must fail fast

== Audience

This documentation targets:

* Integrators who configure platform startup through `platform.json`
* Test engineers who run E2E and inspect artifacts
* Platform maintainers who extend orchestration behavior
* CI maintainers who integrate workflow execution

== Documentation Map

Use this reading order:

* xref:getting-started.adoc[Getting Started] for role-based onboarding
* xref:contract.adoc[Contract and Configuration] for `platform.json`
* xref:architecture.adoc[Architecture] for lifecycle and startup flow
* xref:e2e.adoc[E2E Testing] for test execution and outputs
* xref:ci.adoc[CI and GitHub Actions] for workflow automation
* xref:ui-repository.adoc[UI Repository Integration] for caller-repository setup
* xref:reference.adoc[Reference] for CLI, environment variables, and artifacts

== Contributor Notes

Follow OneCX writing conventions on every page:

* Use active voice and present tense
* Keep headings concise and consistent
* Define acronyms on first use
* Prefer tables and lists for options and procedures
* Reuse repeated content with Antora includes and partials
